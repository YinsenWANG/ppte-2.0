# R4 — 控件层级与认可原型对照

范围：本轮仅处理 R4，承接 R1–R3；验收报告与认可原型不改写。输入为 `docs/audits/2026-09-07-ui-d96f211/sample/source.html`，未添加产品专属文本标记。历史基线 main 为 f5ba190，实际执行父提交为 3a83144（包含流水线前序修复）。

## 实现及验收映射

|报告要求|实现|自动化证据|
|---|---|---|
|文档栏 / 工具条 / 插入层级|提取 `shell-components.ts`，统一隔离外壳样式及 SVG、分组、字体选择组件；移除 workspace 多层 CSS 拼接与 accessibility 强制多行覆盖。桌面文档栏 56px、工具条 46px；放映为蓝紫主操作，下载为具名称与 tooltip 的图标。保留现有统一插入菜单。|`tests/r8-r4-hierarchy.test.ts` 实测高度、强调色；原有四对象插入鼠标旅程继续执行。|
|上下文文字属性 / 颜色 / 精度|字体选择保留作者真实字体栈，另提供系统无衬线、衬线、等宽；字号加减、粗体、斜体同组，对齐同组。颜色控件与快捷色板；混合/透明状态有文字说明。几何只将显示精度收敛至一位小数，不读取后自动写回作者尺寸。布局初始折叠，展开状态在修改后保留。容器对齐使用用户可读选项，列宽规则置于折叠布局中。|真实选择标题、改字体字号 / 加减 / 粗斜体 / 对齐 / 快捷颜色；检查作者计算样式、布局显示精度；下载与独立浏览器上下文重开颜色保持。|
|页面导航与排序|页项保留缩略图、序号与当前页边框；上移下移收于每页更多菜单。原有拖动与 Alt+上下方向键继续可用；禁用边界仍暴露菜单语义。新建页 footer 独立于滚动列表。|鼠标打开菜单移动第1页，真实 Alt+ArrowUp 恢复；UI03 原有首尾边界、撤销重做、拖动、12页回归保留。|
|底栏|页面导航、前后页及页数成组；缩放与帮助图标右置，名称及 tooltip 保留。|尺寸断言、键盘与图标名称/焦点/对比度现有测试；三宽截图。|
|保存反馈稳定 / 错误可读|将完整状态放入一个可展开保存入口，常规状态不新增行。详情保留完整文字；失败或冲突自动打开保存面板，系统原生授权代码不变。|R4 测量修改前后文档栏高度；UI04 保存失败与并发测试、审计保存取消记录。原生授权成功仍 pending。|
|同内容原型对照|1440×960、1024×960、390×844 同一封面标题选中状态。几何数据、审计原始结果与12页截图随附。|[COMPARE.html](COMPARE.html)、`verification/hierarchy.json`、before/after probe 与 followup。|

现有测试因产品交互改变而同步路径：先打开布局折叠区再操作；页面排序先打开菜单；图标按可访问名称定位；色盘按十六进制及透明/混合显示断言。没有删除原有功能断言、force click、隐藏控件或仅内部 Commands 代替鼠标旅程。旧测试中既有内部 API 测试未宣称为新增用户路径证据。

## 四层状态与边界

- code：R4 implemented；生产 Commands、作者 iframe 与保存协议沿用。
- automated：实际命令、测试结果、时间与实现 commit 见 `verification/result.json`。
- real-browser：本机安装 Chrome、headless、离线 file:// 自动化；并非人工操作或原生授权成功证据。
- human：pending，尚无负责人或目标用户视觉评审。对照截图是可审阅证据，不冒充人工认可。

R5 小屏属性区仍覆盖画布，390px 截图明确保留该失败。R6 浏览器 prompt 仍可被原始 followup 观测；本轮不提前实现后续任务。保存权限、Safari、系统 IME、屏幕阅读器、实体触控、媒体按需解码、性能门槛与 PDF N04 根因调查继续交给后续 GAPS / CLOSING，不由本轮自动关闭。

## 复验

`pnpm typecheck && pnpm build && pnpm test`；然后运行未经修改的 `docs/audits/2026-09-07-ui-d96f211/probe.mjs` 与 `followup.mjs`。

`node --test dist/tests/r8-r4-hierarchy.test.js` 生成三宽同内容对照和结构数据到 `artifacts/r8-R4`。生产 CLI 生成的本轮离线样稿位于 `sample.ppte.html`，sha256 与 file:// 冒烟证据见 verification。最终整条流水线交付目录由 CLOSING 轮汇总。

工作区原有 supervisor/brief/日志脚本未删除或提交；以仓库本地 `.git/info/exclude` 精确排除这些既存流水线文件，产品改动与证据全部提交。没有 push。
