# 实施进度

基线：`b8c6d00`。状态：实施中；尚未通过任何完整阶段发布门禁。

## 2026-09-05：C02 首批历史可靠性修复

- 新增显式 `slide.update.unset`，准确恢复原本不存在的可选字段。
- Core 提交前验证序列化后的逆操作能够恢复精确 revision；失败不写日志。
- edit.1 / 操作协议 1.1 声明覆盖项目、浏览器副本、undo/redo、修订补丁与恢复日志；保留旧协议读取。
- 新恢复日志原子升级到 v2；旧版本或无效头部不允许重放新操作。
- 修复修订补丁忽略恢复继承背景的问题。

验证：完整测试 107/107 通过，Host 构建通过；15 项 schema/example 验证、操作一致性和源码 guard 通过。浏览器测试在允许启动浏览器的本机环境运行。

尚未完成：C01 全部基线样本与依赖预算、C02 边界补充验收、C03 历史恢复以及后续功能。不得将上述通过数等同于完整演进计划完成。

## 2026-09-05：C03 诊断与文件恢复首批实现

- 提取 Core 只读历史评估，验证连续 undo 后缀和 redo 前缀。
- 新增 `history-inspect` 与 `history-repair` CLI：另建恢复目录，保留原始包、相邻日志备份、恢复副本与摘要报告。已有目录拒绝覆盖。
- 提供精确 checkpoint 基准重建；逐条前向验证且最终完整 revision 必须一致。
- 快照和资源仍严格验证，不用历史修复绕过损坏资源。

验证：新增 4 项恢复回归通过；完整测试 111/111 通过，Host 构建通过。尚未完成浏览器恢复会话隔离与交互，以及故障注入覆盖；C03 保持进行中。

## 2026-09-05：C05 放映隔离与草稿保留

- 两个编辑入口共用独立于全屏的放映模式控制器；Portable 的修改/undo/redo 持久入口拒绝放映期间写入。
- Host 放映卸载编辑菜单、备注及选中框，内容不可编辑；窗口级快捷键支持失焦后的退出与翻页。
- 放映前提交失败保留文字草稿，输入法组合期间不进入放映；修复 Portable 先删除草稿再尝试提交的问题。
- 编辑态显示全部分步对象，第一正数动画步可退回本页第 0 步。

验证：完整测试 113/113 通过，包含真实浏览器放映/保存/重开、API 写入拒绝、Host 退出和恢复编辑；schema/example 与源码 guard 通过。C05 保持进行中，仍需完整跨入口状态与媒体验收。

## 2026-09-05：C04 实际交付物身份与版本化副本

- ArtifactIdentity 绑定实际脚本、渲染表面、HTML 外壳、资源、字体、undo/redo、能力和兼容 descriptor。构建时间不影响身份。审计会检测内容与身份不一致。
- 只有完整身份相同才复用已有 HTML；源文稿 revision 相同而运行时不同不会误复用。
- CLI 默认显式 versioned-copy，旧 API 缺省保留 error；覆盖必须显式指定且参数冲突拒绝。短摘要同名时继续创建新副本，文件写入保持原子且不覆盖。
- package.json 统一版本为 0.9.0-next.0，生成运行时版本，npm staging 读取同一来源。此为开发版本，尚未发布 npm。
- 浏览器实时 payload 不继续携带旧成品 identity，保存时重新计算。

验证：完整测试 116/116 通过；3 项身份回归覆盖脚本改变、redo-only 历史、旧副本保留及短名冲突；schema/example 和源码 guard 通过。C04 仍需打包安装及发布门禁验收。
