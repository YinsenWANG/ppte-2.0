# C01 frozen failure corpus and reference baseline

The task-start production commit is `dc64f90d857cab01438b73620255ba19823fe1ba`.
It already contains partial C02–C05 fixes. C01 records historical failure mechanisms
and current behavior separately; it does not claim these fixes were implemented here.
All originals are synthetic Apache-2.0 repository materials. No private user files
were supplied, reviewed or published. The manifest hashes exact original file bytes.

## Criterion evidence

- Background / old profile / invalid history / old identity: the first four tests in
  `tests/evolution-baseline.test.ts` independently load fixed files. The empty
  serialized legacy inverse demonstrably fails to restore the original revision;
  the current Operation Engine survives checkpoint/reopen/undo/redo. The 1.0 ZIP
  opens unchanged. Incorrect history is rejected. Identity-less HTML is preserved
  while delivery creates an identified new sibling. The HTML is a synthetic
  identity-less equivalent, not an alleged historical released binary.
- Provenance / hashes / revisions / environment / actual outcomes: the fifth test
  checks every original and frozen report against `tests/fixtures/evolution/manifest.json`.
  Intentional bad-history fixture construction is test input, not a production write path.
- Browser protocol / dependency budgets: the sixth test checks profile limits,
  frozen incremental thresholds, E02's version reference and all raw sample counts
  and recalculates p50/p95. `m1-dependency-budget.json` specifies comparison rules.
  Limits are engineering constraints fixed before candidate selection, not a claim
  that an unimplemented candidate has passed them.
- A06 remains covered by the existing `tests/portable-presentation.test.ts` and
  the recorded blackbox Host/Portable cases. C01 does not certify all of A01–A20.

## Reproduction

Run from the repository root:

```sh
pnpm typecheck && pnpm build && pnpm test
node --test dist/tests/evolution-baseline.test.js
node scripts/evolution-baseline.mjs /tmp/c01-browser-rerun.json
pnpm blackbox:final
```

`node scripts/evolution-fixtures.mjs` deliberately regenerates the three synthetic
originals from the current build. Do not use regenerated bytes to silently replace
frozen provenance; a changed corpus needs a new manifest version and hashes.
Tests only write temporary output directories and preserve fixture bytes.
Browser reruns default to `/tmp/c01-browser-baseline.json`, preserving frozen data.

The browser reference uses headless Chromium on the recorded Apple M4 machine,
1440×900, file URLs and 12/30/100-page synthetic text/shape decks. Each deck has
20 fresh-process startup samples, 20 same-context reload samples and 30
Operation-Engine edit-to-two-animation-frame samples. Fresh process does not mean
OS disk-cache cold. This measures API-to-paint, not keyboard/IME latency. System
Inter fallback is explicit; no embedded font bytes or media are included. Fonts
and image readiness are awaited. It is not a real-Safari, lower-end-device,
Host-startup, physical-IME, or complete interaction/memory qualification.
E02 must compare candidates on the same setup and separately prove correctness;
future A20 scenarios are explicitly listed in the protocol.

The start blackbox report contains 67 green and 2 red cases. Existing reds are
`section-41:1` (Host download timeout) and `delivery:2` (editable browser journey).
They remain release risks; no assertions or gates are disabled to accommodate them.
