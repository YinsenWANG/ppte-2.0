# Q02 candidate and rollback closure

Q02 remains **partial**. D07 and P01 are partial and the direct dependency Q01 is
partial. Actual client acceptance and the product-owner/two-user A21 panel have
not been supplied. The automated candidate contracts below pass; they do not
close the release acceptance gap. `pnpm release:check` intentionally exits 1.

## Criterion evidence

| Criterion | Result | Concrete tests and retained evidence |
| --- | --- | --- |
| Stage tasks/blockers zero; npm/Skill/offline reproducibility | **Blocked** for release; automated candidate contracts pass | `tests/release-candidate.test.ts` checks incomplete/missing/unevidenced stage tasks, unverified clients, corrupt evidence and changed builds; packs the same staged bytes twice and compares SHA-256. `tests/npm-native-skill.test.ts` installs an actual tarball and runs compile/inspect/preview/commit/reopen/undo/redo/deliver from installed Skill instructions with Node networking blocked. |
| Upgrade/rollback preserve original, old tarball, profile, reports; no automatic downgrade | **Pass** for the synthetic candidate drill | `tests/release-candidate.test.ts` installs the retained C06 tarball and current candidate into isolated consumers, preserves original/old HTML, delivers a versioned HTML copy, opens a restored original with the actual old CLI, rejects newer schema without mutation, and checks retained byte digests. Retention rejects missing inputs, existing output, unsafe paths, incomplete receipts and corrupt bytes. `tests/history-repair.test.ts` covers real recovery failures, original/journal/CAS preservation and crash points. |
| Git/npm/local install/HTML upgrade recorded separately | **Pass** | The release action contract rejects missing action records or evidence-free pass. `release-manifest.json` records Git push/tag and npm publication not-run; local runtime/Skill installation passed only in temporary consumers; HTML upgrade passed only for a synthetic legacy project. Persistent user installation and user HTML upgrade are not-run. |

`q02-drill-report.json` binds the application/runtime identities, source profile,
revision, archive digest and HTML ArtifactIdentity. `q02-retention-receipt.json`
records each preserved byte digest/size. Retained artifacts are in
`artifacts/q02-drill-final`; the original old tarball and its C06 report/provenance
are committed under `tests/fixtures/evolution/q02` so the drill does not depend
on the historical `/tmp` location. The two tarballs have the same application
semver but different runtime builds: semver alone is not release identity.

To reproduce, build and follow
`skills/ppte/references/release-migration.md`, selecting a fresh Q02_OUTPUT path.
The receipt's `.retained` tarball bytes can be copied to a fresh `.tgz` filename
before a manual `npm install`; their SHA-256 must remain unchanged. No source
project profile is ever manually lowered. The new table-profile fixture is
written by the public checkpoint writer and is rejected by the historical
reader at schema validation; no old-reader read-only preview is claimed.

## Remaining release work

- Complete D07's A21 ten-task panel with product owner and at least two target users.
- Complete P01's real device/Safari performance evidence against frozen budgets.
- Complete Q01's actual Safari/Chrome and Office open/edit/resave/visual evidence,
  bound to the current artifact identity, then close its dependency status.
- Rebuild/revalidate a candidate when code or runtime identity changes, retain
  its package/reports, and promote G0 → G4 only when all blockers are cleared.
- Execute publication or persistent installations only within actual task
  authorization. This task prohibits push; no npm release was attempted while
  release acceptance is blocked.

Initial npm dependency resolution can use the network; offline CLI verification
blocks Node network APIs, not the operating system network. The pack check is
repeatability of identical staged bytes, not a claim of hermetic rebuilds across
machines. Hashes prove byte integrity, not publisher identity or human actions.

## Final verification

`pnpm typecheck`, `pnpm build` and `pnpm test` exited 0: **392 passed**, zero
failed/skipped, versus 387 at task start (**+5**). `pnpm blackbox:final` exited 0:
**72 green / 0 red**, identical to task start with no new reds. Raw-log digests
and paths are in `q02-verification.json`. The native Skill journey and rollback
drill use the same candidate SHA-256. `q02-release-gate.json` records the expected
blocked release check (exit 1), separately from passing automated verification.
The implementation commit is GPG-signed and contains the required signoff;
TASKS.json records its full hash without marking incomplete acceptance done.
