# C06 — M0 clean-install candidate

Implementation: `76a5df4e88bf408b5eee014f8dd07107aeb6433a` (signed and signed off).
Application: `0.9.0-next.0`.
Runtime buildId: `1498765a8e827ac7991f0009361bb371e814d9ce42e22346d3d0400284eb6d96`.

## Criterion evidence

1. `tests/npm-native-skill.test.ts`, first test: a real npm tarball is installed into a temporary consumer using a fresh cache and home, without workspace dependency links or optional Playwright. The installed Skill's JSON example drives compile → inspect → preview → commit → reopen → undo/redo → deliver. Node network APIs are blocked with a negative probe, credentials are absent, and MCP is not started. Delivery identity, runtime buildId, revision and unchanged repeated output are asserted. Missing PDF renderer produces installation instructions without modifying the source or creating an output.
2. The second test runs 57 existing M0 fixture/compatibility/recovery/identity/presentation/workflow tests and verifies the packaged runtime bytes match the tested build. The installed package file hashes and fixture manifest digest are retained in [candidate evidence](c06-candidate-report.json). This uses the public synthetic failure equivalents from C01; no private originals were supplied. A blackbox subgroup verifies report build binding. Full [final blackbox evidence](c06-blackbox-final.json) matches that build and remains 69 green / 0 red against the [start baseline](c06-blackbox-start.json).
3. The third test verifies four independent release-action records and rejection of an existing Skill destination without modifying it. Git push and npm publication are `not-run`. The tarball and temporary Skill installation pass; persistent user Skill installation is `not-run`.

Verification: `pnpm typecheck && pnpm build && pnpm test`, then `pnpm blackbox:final`. All pass: 154 → 157 tests (+3), 0 failed/skipped; blackbox 69 → 69 green, 0 reds. The targeted 57 tests are rerun inside the new test and are not counted again in the +3 delta.

## Candidate and migration

Retained local tarball: `artifacts/c06-candidate/ppte-cli-0.9.0-next.0.tgz` (984922 bytes).
SHA-256: `3ebf2cf8d727577681ff062102e4a3e1abc02bfa31544f62a88c14dda7840a53`.
This ignored build artifact is available in the working environment, not published to a registry or committed to Git. Regeneration records a new tarball digest; do not assume identical archive bytes.

[Packaged migration notes](../../../skills/ppte/references/m0-migration.md) cover installation, conflicting Skill destinations, old/new profiles, history recovery, derived HTML copies and rollback. Only verified suffixes or reconstruction from an exact base may recover history. Preserve original projects, journal/CAS and previous runtimes; replacing a runtime does not downgrade newer files.

Remaining limits: installing required fflate can use the network; runtime offline evidence uses blocked Node APIs rather than an OS network namespace. Manual model-host evaluation, real Safari and physical-device performance remain unverified. No Git push, npm publication, persistent user Skill installation or automatic upgrade of existing HTML was performed.
