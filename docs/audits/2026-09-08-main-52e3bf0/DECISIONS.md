# 审计跟进（audit-followup）— 收尾裁决

## 裁决：管道收尾与第 4 项归属（2026-09-08 23:23，用户授权助理代为裁决）

用户指令原话：「你帮我裁决吧」（承接管道结束播报「管道已结束，需要你裁决或我介入」）。

### 裁决内容

1. **认可自动化范围完成**：R02 / R01 / R03 / DLV 的 code + automated 层通过。编排者独立复跑（非采信 agent 自述）：`pnpm typecheck` RC=0、`pnpm build` RC=0、`pnpm test` RC=0（239 tests / 223 pass / 0 fail / 16 skipped，与 result.json 一致）；交付稿 SHA256 = `4cf0ddbfcc4f6cd3d1e1a713a0e60fbde703f43adb0533cc13737af7a2bba897`，与 result.json 一致；编排器独立打开成稿：标题正确、PDF 入口禁用、无诊断标题/插图。**native / human 两层不标 done。**
2. **第 4 项「原生保存验收」保持 pending，不新增机制**：本机无后台桌面控制通道；前台 GUI 自动化会抢焦点，且系统级保存/授权对话框本身属于需人工在场的动作，收益不抵风险，不引入。改为并入用户一次验收会话执行（见下方三步清单）。**禁止**以注入句柄、下载文件、截图或状态文案冒充原生写回。
3. **交付**：唯一干净九页成稿 `evidence/DLV/delivery/Cherry-Studio-开源之路.ppte.html` 作为本稿交付用户逐页评审。原九页设计方向不变，等用户逐页反馈。
4. **落地**：`work/audit-followup` 已验证提交 ff 合入 `main` 并推送。与既往 U/F 管道一致，main 为唯一集成线。
5. **PDF**：维持 U04 选项 1 —— 未完成、不新增依赖、不进入产品集成。
6. **下一轮**：等用户逐页反馈与第 4 项结果后再开；不以新增报告代替关闭缺陷。

### 补充事实（编排器实测，供用户验收参考）

- 在 Chromium（file://）实测：`isSecureContext === true`，`typeof showOpenFilePicker === "function"`，`typeof showSaveFilePicker === "function"`。即本机 Chrome 打开本地 HTML 时，原生「选择当前文件并保存」通道**存在**；仅系统选择器的人工交互未验。

### 对后续执行的硬约束

- U01 保持 `full-deck-awaiting-user-acceptance`；U02 / U03 / U04 / U05 状态不变；本轮不改变既有裁决。
- 不新增依赖 / 服务 / 上传；不改设计方向；不擅自放宽 PDF 要求或弱化断言。
- 第 4 项与九页签收由用户侧执行。任何真机结论必须附用户提供的时间、路径、文件前后 SHA256 / 录像，不得虚构。

## 用户验收三步清单（第 4 项 + 九页签收）

1. 把成稿下载到本地固定路径（例：`~/Desktop/Cherry-Studio-开源之路.ppte.html`），用桌面 Chrome 以 `file://` 打开，翻完九页；记下要改的页与问题。
2. 点「编辑」改一处文字 → 选图裁切一次 → 点「保存」→「选择当前文件并保存」→ 系统选择器里选中**同一个文件**并授权；看到「已保存」后**关闭整个 Chrome 进程**，重新打开同一路径，确认文字、裁切、原图都在。
3. 把每步结果（时间 / 截图或录屏 / 文件前后 SHA256）发回。失败、拒绝授权、冲突也照发——保留现场，不要重试覆盖后再报。
