# main bf7d6ea 复审

2026-09-08。审查基线 bf7d6ea4bb00df85ebe14da9c8d5d8099cf1c870，对照 5987af6 功能收敛方案及 b93ffed 历史问题。结论：核心编辑流程改善，尚不满足完整验收/发布要求。此次未安装新 Skill、未重做用户成稿、未修改产品实现。

## 本地独立验证

构建 `node scripts/build-html.mjs --clean`、`tsc -p tsconfig.build.json` 和 `tsc --noEmit` 成功。复用上一审查工作区的 node_modules，仅作为本地检查环境，未改依赖声明。Chrome headless file:// 聚焦测试 `node --test dist/tests/focused-product-f*.test.js`：25 passed、0 failed、0 skipped，原始输出见 focused-tests.log。首次沙箱内 Chrome 启动失败，允许启动浏览器后重跑成功；不是产品故障。本次没有重新执行全仓测试，远程所报 149 passed / 16 skipped 不作为本地复跑结果。

覆盖：单图片入口、旧内容和历史数据保留、取消/拒绝不下载、多行 Enter 与内联格式、四类布局的图片操作/裁切/替换、撤销/重做、下载/新进程重开、响应式/40页导航、离线10页放映。原生文件句柄测试仍是模拟，下载不等于原文件保存。

审阅 F05 edit-1440 证据截图：顶栏与编辑条明显精简，画布居中，保留统一侧栏与样式；不是本次用户人工确认，也未声称重跑所有视觉对照。

## 尚未完成

1. **P1 PDF 未实现。** workspace.ts 创建 disabled PDF 按钮；已移除系统打印产品调用。F04A 对比浏览器内 pdfmake 转换与受控 Chromium，前者布局/字体及页数失败，后者留有 PDFKit 文本搜索/顺序与复杂变换差异，人工选区未验。禁用功能诚实，但不等于完成。只证明本轮候选未合格，不证明所有路线不可行。进一步先定位 Chromium 失败的内容/阅读器/渲染适配原因，再讨论是否继续限定验证；不得静默转整页图片或必需服务。
2. **P1 原文件保存/自动保存没有原生闭环证据。** 代码错误分类、队列保护、显式下载和草稿缓存改善已核对；需目标桌面实际选择文件授权、写回、关闭浏览器、重开原文件，并验证取消、冲突和权限失效。不能以 headless 下载/模拟句柄关闭此项。
3. **P2 B2 安全引用链接仍未修。** packages/html-document/src/content.ts 的 href 分支仍只允许片段链接，普通 a[href=https://…] 继续报告 CONTENT_URL_REMOVED；F06 原始带链接样本也记录 open。按历史合同区分显式安全导航与自动网络加载，不放宽脚本/危险 URL 防护。
4. **后续建议未落地，不冒充实现缺陷。** 演讲者弹窗、复杂属性、复制恢复 HTML 等仍保留；远程明确未把额外删减建议视为逐项已确认。若下一轮执行这些删减，应在交接明确采用范围，保留作者内容与内部保护。
5. **证据缺口继续保留。** 系统 IME/粘贴、Safari/触控/无障碍、原生保存、大图草稿压力、参考机性能及 UI/成稿人工确认。不要扩写成这些功能均已坏；目前是没有证据。

## 修复确认与性能判断

B1 普通 Enter 已在 beforeinput 中转换为行内换行，多行粘贴转为纯文本与 br；本地真实浏览器键盘测试通过，可关闭原复现的自动化层，系统输入法层仍待验。图片增加真实比例、进度、取消及切换场景终止，聚焦测试通过。历史自动新增暂停，草稿按修订/基线复用，属于实际减负；持续编辑仍同步全量草稿写入，不能宣称大稿性能全面通过。

建议后续优先：B2 与原生保存实测 → 明确额外删减范围 → PDF 失败原因与技术路线决策 → 用户确认 UI。保持同稿与真实旅程证据，不再用新增报告数量替代完成缺口。
