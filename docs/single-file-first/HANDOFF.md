# 给远程 Codex 的总交接

请根据本目录 [PLAN.md](PLAN.md) 和 [TASKS.json](TASKS.json) 继续实现 PPTe。本方案已合并用户四项人工反馈和 [ea260ce 审查的五项问题及证据](../audits/2026-09-06-html-first-6091c9a/REPORT.md)，不需要另起一轮总体方案设计；具体实现细节按代码和测量作决定。

先读取本提交及审查 `ea260ce678019c4e88c0ff3a9db4867a311e0b59`，再与开发分支当前代码比较。审查对应产品代码 `6091c9a73e75b2a51b00bef19b61d58e2dbf52a1`。若已有修复先验证并映射，保留后续有效代码，不重置分支或重复重写。历史审查证据不可回写成通过。

本轮四条主线：

1. **单个 `.ppte.html` 直接打开就是作品、编辑器和放映器。** 不需要用户安装 Node 或启动 `127.0.0.1`。当前代码已内嵌编辑器，应修正入口和依赖方向，复用已有核心。原文件自动保存受实际浏览器授权限制；支持时授权写回，不支持时仍完整编辑并下载更新后的同类文件。下载/草稿不得称为原文件已保存，不能以必需服务作为降级路径。
2. **审美是提醒，不是模板或硬约束。** Skill 只添加 PLAN 中的小段审美与视觉理解提醒；保留模型原生 HTML/CSS 的自由，不新增配方、卡片数量、机械字数或美学评分门禁。
3. **优化真实等待与编辑响应。** 先测量，重点检查全量缩略图刷新、每按键整稿同步草稿、重复通知与重复媒体编码；复用安装、缓存资源、并行独立 I/O。发布验收与日常生成分开，不能省略正确性或编造 token 数据。
4. **先看懂图片再选图和排版。** 当前 Agent 使用视觉能力理解用户图片，按页意图选用、合理裁切；浏览器离线支持插入、替换、焦点和重置。资源保持单文件内嵌、可恢复且不重复膨胀，不给最终读者增加云端 AI 依赖。

先完成 S00–S03 的合同、入口、保存与编辑修复；测量和轻量 Skill 工作可在清楚边界下并行准备，然后完善图片能力与整体验证。不能把 F01–F05 丢掉，也不要恢复用户已经退休的 Office/PPTX/旧 IR 功能。待修复点、源码范围和每项验收已写入 TASKS.json。

每个可审查阶段提交代码和对应证据。状态区分代码实现、自动化、真实浏览器与人工确认；无法获取 Safari、真实文件选择器、用户评审、低性能设备或模型遥测时，保留 pending 和具体原因，先完成不依赖它们的工作，不虚构通过。

最终回交：修复 commit ID/链接；四项用户反馈、F01–F05、R01–R06 的逐项状态；测试与原始证据；实际新版 Skill 生成的一个 `.ppte.html`。给用户的主验收入口必须是文件，不能再默认发 localhost 链接。全部真实通过后再讨论下一阶段。
