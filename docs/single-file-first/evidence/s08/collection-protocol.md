# 待采集协议（不是执行结果）

S08 保持 partial；以下角色尚未指定实际人员，不代表已经邀约或取得同意。负责人安排评审，测量执行者提供真实环境与 provider 原始回执。不得由 Agent 填写人评，也不得把等待槽位计为通过。

## 同模型九对与图片稿

[controlled-nine-pairs.json](controlled-nine-pairs.json) 固定保留 cherry / product / data 三份历史材料与摘要，每份三对共十八次生成槽位。材料反映其历史版本，提示要求标明设计目标；不得偷换成当前事实。旧 manifest/baseline 不更改。开始前冻结新版 Skill、代码、材料、字体、参考硬件、实际模型版本和 reasoning 配置；双方内容提示相同，增强侧增加新版 Skill。保留盲化对应表，交错运行 direct/enhanced，避免缓存/顺序偏差。模型身份必须用实际 provider 回执验证，不能从当前会话自称推断。

每次保留 run ID、原始提示、起止时间、冷/热状态、安装、材料理解、模型生成、增强、检查、修正、总时间、原始模型调用及 usage 回执（输入、输出、缓存、视觉 tokens 分开）、首稿 HTML、全部失败稿和错误、最终文件、截图、重试及尾部。缺测字段 null + 原因，不能填零；失败也保留，不能只选最快成功运行。两侧复用安装和已验证缓存，冷安装单独报告。禁止用增强时间代表总时间或按文件长度估算 tokens。

完成九对才计算热端到端时间中位数之比（增强侧中位数 / direct 中位数）及 tokens 中位数之比，分别要求 ≤1.25 和 ≤1.20；同时展示逐对比值、所有原始值与尾部，分母缺失/为零时不得报告通过。统计口径在采集前冻结，任何变更留版本记录。图片稿另开独立组，记录视觉输入、预处理、编码、解码阶段，不合并进纯文本中位数。没有用户原始图片时保留 pending，不用 S05/S06 合成代理图冒充。

## 负责人 + 两名目标用户

[real-user-feedback.json](real-user-feedback.json) 当前三份回执均为空。需真实身份/角色确认、时间、浏览器/设备、文件哈希、盲化 A/B 顺序、逐对原话与“优于/相当/逊于/无法判断”、理由及问题截图。建议采集前由负责人确认三人多数意见作为一对的审美结论；未确认规则或回执缺失仍 pending，不能由 Agent 裁决。九对至少六对不逊于 direct。

事实错误、主体/关键文字裁切和保存失败分别记录并独立否决；审美票数不能抵消功能失败。人工在实际文件上检查修改、撤销/重做、保存状态、下载重开、放映和可读性。原生授权另记录取消/拒绝/授权/撤销/再授权、写入读回和磁盘哈希；Safari、系统 IME、打印对话框与无 Node 目标设备各留操作证据。Agent 看图、Playwright 输入和模拟 handle 不能填成人工操作。未发送任何邀约；下一位执行者需在取得明确发送授权后联系评审者。

## 真实低性能设备

[real-low-performance-device.json](real-low-performance-device.json) 没有实机样本。需真实设备型号、CPU、内存、OS、浏览器版本、电源模式、温度/负载、代码与文件摘要；冻结参考机和低性能机，使用同一基线/修复版本与固定材料。CPU 限速仅可另列模拟，不可替代实机。

分别测 12 页轻稿、长稿、图片稿。至少 30 次输入到下一帧与翻页、10 次停顿到确认保存，保留逐次时间、首次可见/可编辑、缩略图、序列化、实际落盘、失败/重试、媒体原始/派生/HTML/gzip 字节及历史内存。工程目标为输入 p95 ≤50 ms、翻页 p95 ≤100 ms、真实授权保存含防抖 p95 ≤1.5 s；低性能机另报告相对基线改善和失误。S04 开发机输入/翻页未达标的旧结果原样保留。不能删图、缩小事实文字或关闭持久化来过线。

## 关闭条件

每份证据追加实际采集者、原始文件与 SHA-256、运行版本提交、四层状态。保持历史记录，只新增本轮结果。全部 acceptance 实际通过且负责人确认后才能另一次更新 S08 为 done；当前不能宣布全部完成或开始下一阶段。
