# U03 原生桌面回交步骤

状态：pending。用户已拒收仍 reopened，尚未获得本轮用户签收。工具列表无桌面控制通道；本机 System Events 查询挂起后停止，无可用的原生选择器/权限操作证据。不得将此解释成保存失败原因。Chrome headless 与模拟句柄结果见 verification/result.json。

使用 `Cherry-U03.ppte.html` 的独立副本，先记完整路径、系统/浏览器版本、文件 SHA256 和正文。保持原文件供对照，不覆盖用户已有稿。

1. 桌面 Chrome 直接打开副本（file://）。进入编辑应无授权提示。录屏记录是否容易找到“保存”，修改标题及图片裁切。
2. 点击保存，应先说明首次需选择当前文件；点“选择当前文件并保存”，在系统选择器选这一副本并授权。记录目标完整路径、权限界面和“已保存 · 时间”。在终端校验文件已变更、正文/裁切已写入。
3. 再编辑，不按保存，等自动保存确认；再记 SHA256 与正文。退出整个 Chrome（不是只关标签），重新打开同一路径；文字、裁切、图片原资源及默认阅读模式应保持。重开不能假设旧授权仍有效。
4. 分别取消选择器、拒绝权限、撤销已授权权限，确认无自动下载/循环弹窗，编辑仍在。用户点保存后才重试。
5. 用同名但不同内容/身份副本验证错文件拒绝；关联后用外部编辑器修改目标验证冲突。磁盘稿不被覆盖，当前编辑保留；按需预览磁盘版本、重新选择或明确下载。不要用浏览器内变量注入代替这步。
6. 在慢写入、写入失败可真实重现的目标上记录保存期间继续编辑和失败后重试，确认新修订未被旧确认清除。若无此设备条件，单项 pending，不伪造故障。
7. 未保存退出/重开时预览恢复草稿：记录时间与来源，取消或“保留文件版本”不改变当前正文；恢复后仍需落盘。大图情况下检查连续输入、恢复缓存不足提示与手动保存。
8. Safari/内置浏览器以实际能力记录主按钮；不支持时点击一次只下载一个完整 HTML，原文件未覆盖。不得把下载重开算作原文件写回验收。

每步留截图/视频路径、SHA256、前后正文/裁切、操作时间、平台、结果；原生与模拟证据分开。用户最后分别确认保存入口、操作顺手程度和数据可靠性。未完成项继续保留具体 pending 原因，不修改历史拒收事实。
