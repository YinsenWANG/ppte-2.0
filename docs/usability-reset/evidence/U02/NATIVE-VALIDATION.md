# U02 目标机复验（全部 pending）

起点：用桌面 Chrome 直接打开本目录 `Cherry-U02.ppte.html` 的副本。该文件只更新运行时，不代表 U01 美学签收。不要覆盖现有用户原稿。

1. 阅读→编辑→插入竖图。确认当前页能看见且已选中；取消文件选择、读取取消、无效文件分别不改变原稿。再插横图、透明图、小图和较大图。
2. 鼠标从未选中图开始拖动；分别拖四角，看比例和命中。放大画布、滚动、改变窗口尺寸再操作。触控每个角复验。观察内容兄弟不跳动、图片不能彻底丢到页外。
3. 双击/裁切：框外原图是否可理解；拖动主体、拖框角，滚轮缩放主体。Esc 和取消应原样恢复；Enter/完成后只需一次撤销。连续几次调整不得重新编码原图。
4. 替换图片确认框位置/尺寸不变；显式删除及 Delete 能撤销。文本输入、中文输入法、阅读、放映均不能误操作图片。
5. 在 U03 支持的原生关联流程中选择原文件，实际手动写回一次、自动写回一次。记录文件前后摘要与裁切，然后退出整个浏览器，重开同一磁盘文件，核对文字与图片裁切。Playwright 下载或模拟句柄不可代替此步。
6. 用户确认三个短动作是否易找、拖动/缩放/裁切是否顺手；反馈仍未收到，不能标 human passed。

每项记录系统/Chrome版本、屏幕/缩放、原生能力、文件摘要、截图或视频路径与 SHA256。若失败，保留步骤和文件，不推测权限或故障原因。自动化对应项见 IMPLEMENTATION.md；未执行的目标机项均为 pending，理由是当前没有桌面操控/原生选择器通道，且尚无用户复验结果。
