# F03 原生与人工验证：全部 pending

原因：本次可用通道为 macOS 上 Chrome headless + Playwright；没有物理桌面原生文件选择器控制、独立目标设备或用户操作回执。下列步骤不能由自动化事件、DOM 断言或截图替代。

1. 桌面 Chrome 双击 Cherry-F03.ppte.html，确认默认阅读；进入编辑，从系统图片选择器分别选横图、竖图、透明大图并取消一次。核对位置、选框、比例、错误文案与取消无副作用。
2. 在实际作者文稿及四份 samples 中，用鼠标/触控移动、调整尺寸；流式布局用顺序/对齐。确认可理解且不会遮住重要文字；没有空白时应显示准确错误。
3. 替换图片（含当前浏览器支持的 AVIF/GIF）、取消/无效文件重试、裁切/焦点/重置；用真实照片检查主体和关键文字，不能把工程渐变图当语义选图通过。
4. 撤销和重做全部图片操作；大图读取过程中取消或切页，确认没有残留和错误插入。记录设备、浏览器版本、操作视频、原图大小/像素与结果。
5. 依 F02 原生清单：授权原文件→手动/自动保存→检查磁盘→完全退出浏览器→重开，核对裁切框、原图、说明、位置；失败时明确下载并重开。当前仅下载分支有自动化实证。
6. Safari/Firefox、390 小屏/物理触控、200% 缩放与低性能设备分别验证支持范围。最终图片主体、透明效果、界面可发现性及整稿美学由用户确认。

每步记录 code / automated / real-browser / human 各层；本文件所有 real-browser/human 项在收到证据前保持 pending。
