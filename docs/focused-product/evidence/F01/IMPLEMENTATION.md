# F01 — 移除废弃入口并收敛菜单

F01 实现范围：只插入图片、一级 PDF 入口、已有作者内容及历史兼容。以 5987af6 的 PLAN/TASKS/PDF-AND-CORE-REVIEW 为合同；已读取 b93ffed 在 `docs/audits/2026-09-07-main-d1db13d/REPORT.md` 中的未关闭审查及 UI DESIGN。历史审查文件与认可原型未修改。

- 编辑工具条的“插入图片”直接打开本地选择器，移除四类插入菜单及 T/t 快捷键。底层 Commands 与已有文本、形状、表格、SVG、媒体编辑/阅读/放映/保存能力保留；没有把废弃入口挪到隐藏菜单。
- 顶栏移除“更多”，阅读/编辑均显示“导出为 PDF”。按钮 **disabled**，标题说明尚未可用、路线正在验证。移除产品运行时的 html-print 导入、PPTePrint 和 beforeprint/afterprint 注册。旧 html-print 源码留作历史对照；不打包进单 HTML。**F04A/F04 未完成，没有可交付 PDF，也没有架构选择或变化。**
- 版本历史、命名/恢复版本、容量管理、去历史下载、新文件实例入口移除。默认不再在首次编辑、下载、文件适配器或 loopback 保存时生成历史快照；普通文件也不凭空生成空历史索引。旧 history-wire、资源池、损坏/未知索引安全保留。没有通过清空版本数据实现减法。
- 阅读顶栏收起保存控件；编辑中的保存状态承接文件状态、重读/恢复/冲突保留与一个明确下载选择。删除常驻下载及重复下载按钮；撤销/重做不受历史 UI 移除影响。保留原中性工作台、蓝紫模式/主操作、紧凑分组、独立页面栏和响应式属性区；窄屏 PDF 替代原“更多”位置。

## 验收与证据

`verification/run.mjs` 顺序执行 `pnpm typecheck`、`pnpm build`、`pnpm test`；结果、日志、实际测试代码提交、时间、平台及逐 acceptance 四层状态见 `verification/result.json`。复验：`node docs/focused-product/evidence/F01/verification/run.mjs`。

`tests/focused-product-f01.test.ts` 的七项测试覆盖直接图片选择、空选择无副作用、撤销重做、外层及 iframe 中的废弃按键无效果、无旧入口、新 PDF 边界、四类历史文件编辑下载后关闭整个浏览器并新进程重开、模拟文件适配器的真实磁盘写入，以及原审查 Cherry 十页稿升级与三尺寸截图。图片选择由 Playwright 捕获 filechooser/setFiles，**不称为原生系统选择器人工验证**。适配器测试明确使用 mock 权限/句柄，不冒充真人授权写回。

旧稿增强前后正文精确相等。浏览器模式切换可能使 CSSOM 将内联声明的空格/分号标准化；对应测试保留全部 DOM、属性与 CSS 属性/值/优先级，仅先标准化内联声明字符串再比较，不忽略样式。新增合成内容夹具的下载/重开正文及历史直接严格比较。

旧菜单/系统打印要求被新用户范围覆盖，测试的退休、保留断言与迁移逐项列在 `TEST-MIGRATION.json`。退休测试显示为 skip，不计作通过。不把旧菜单操作换成内部 Commands 后仍宣传“产品插入成功”：非图片对象仅作为明确标注的 command fixture，其后真实属性、移动、撤销、保护及下载重开断言继续执行。既有 PDF 色彩正/负样本阈值保留。

最终截图在 `screenshots/`，可直接打开的 F01 运行时样稿是 `Cherry-F01.ppte.html`；它是原审查稿的升级副本，不覆盖原稿。样稿只用于 F01 UI/兼容验证，不表示 F02–F06 或 PDF 已完成。人工观感、原生桌面操作及其他浏览器未取得证据，四层状态保持 pending，不能从截图/全绿推定通过。

## 后续未关闭与额外能力删除

B1 Enter 后文本宿主能力丢失仍交 F02；B2 安全 HTTPS 链接仍未关闭。原生文件授权/撤销/跨会话保存、系统 IME、图片完整闭环、PDF 验证与实现、最终 UI/整体验收按原任务继续；F01 未改写其状态。保存失败自动下载等旧问题也未在本任务伪报修复。

**额外产品能力移除：无。** 独立演讲者弹窗/快捷键、页面新建/复制/重排、已有对象属性和草稿冲突保留操作未按“建议”擅自删除。当前提交未引入服务、联网上传、运行环境或新包依赖。

工作区启动时已有六个管道调度文件（SUPERVISOR9_STATUS.md、brief9-F01.md、push-watcher9.sh、sup9-common.md、sup9-run.log、supervisor9.py）；保留原位，仅列入本 checkout 的 `.git/info/exclude`，不提交、不删除或改写它们。
